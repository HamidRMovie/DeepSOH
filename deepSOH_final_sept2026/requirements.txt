# Python packages for the simulations in sim/, at the versions the data was made with
# (Python 3.9.13, Windows 11). From the repository root:
#   pip install -r deepSOH_final_sept2026/requirements.txt
# Do not install PyBaMM from PyPI: the scripts use the modified PyBaMM in ../pybamm.
numpy==2.0.2
scipy==1.13.1         # below 1.14 reproduces the data exactly (the model uses interp2d)
casadi==3.7.2
pandas==2.3.3
matplotlib==3.9.4
sympy==1.14.0
anytree==2.13.0
autograd==1.8.0
scikit-fem==11.0.0
pybtex==0.25.1
setuptools==78.1.0    # provides pkg_resources, which the bundled PyBaMM imports
